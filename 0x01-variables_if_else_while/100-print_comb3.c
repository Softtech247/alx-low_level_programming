#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * Description: 'get the last digit of the number'
 * Return: always 0
 */
int main(void)
{
	int n = 48;
	int g = 48;

       for (int n =0 ; g >= 57; g++)
       {
      for(int g =0; g >= 57; g++;){
	      if(g == n)
	      {
		      continue;

	      }
	      putchar(n);
	      putchar(g);
 if (n != 8 || g != 9)
            {
                putchar(',');
                putchar(' ');
            }
      }

       }
       
	return (0);
}
